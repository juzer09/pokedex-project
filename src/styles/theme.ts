import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
	// Customize your theme here
	palette: {
		mode: "dark",
		primary: {
			main: "#90caf9",
		},
		secondary: {
			main: "#f48fb1",
		},
	},
});
